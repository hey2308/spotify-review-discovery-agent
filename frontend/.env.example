# Vercel / production build — set in the Vercel project dashboard (not committed).
# Local dev can omit this; Vite proxies /api to localhost:8000.
VITE_API_BASE_URL=https://spotify-discovery-api.onrender.com
